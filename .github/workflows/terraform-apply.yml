name: Terraform Apply (main)

on:
  push:
    branches:
      - main
      - development

permissions:
  contents: read

jobs:
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name == 'main' && 'PRD' || 'DEV' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.12.0

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="prefix=${{ secrets.ENVIRONMENT }}"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform apply
        env:
          TF_VAR_environment: ${{ secrets.ENVIRONMENT }}
          TF_VAR_unique_id: ${{ secrets.UNIQUE_ID }}
          TF_VAR_gcp_project_id: ${{ secrets.GCP_PROJECT_ID }}
          TF_VAR_snowflake_org_name: ${{ secrets.SNOWFLAKE_ORG_NAME }}
          TF_VAR_snowflake_account_name: ${{ secrets.SNOWFLAKE_ACCOUNT_NAME }}
          TF_VAR_snowflake_private_key: ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
        run: terraform apply -auto-approve -input=false
